name: CI

on: [ push ]

jobs:
  test:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - run: composer install --no-interaction --prefer-dist --require-dev
      - run: ./vendor/bin/phpcs
      - run: ./vendor/bin/phpunit
