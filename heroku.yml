build:
  docker:
    backend: Dockerfile
    frontend: nginx:alpine
run:
  frontend: -C ./docker/nginx.conf ./
  backend: php-fpm -F
release:
  image: backend
  command:
    - php bin/console doctrine:migrations:migrate --no-interaction
